apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: confluence
  namespace: default
spec:
  interval: 10m
  chart:
    spec:
      chart: confluence
      version: 1.22.4
      sourceRef:
        kind: HelmRepository
        name: confluence
  values:
    env:
      - name: COREY_VAR
        value: "corey p"
    replicaCount: 2
    ingress:
      enabled: true
      hosts:
        - host: confluence.example.com
          paths:
            - path: /
              pathType: Prefix
